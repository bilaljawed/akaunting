{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "rm -rf bootstrap/cache/*.php && php artisan package:discover && php artisan config:clear && php artisan serve --host 0.0.0.0 --port $PORT"
  }
}