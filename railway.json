{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-dev --no-scripts --optimize-autoloader"
  },
  "deploy": {
    "startCommand": "rm -rf bootstrap/cache/*.php && php artisan migrate --force && php artisan config:cache && php artisan event:cache && php artisan serve --host 0.0.0.0 --port $PORT"
  }
}